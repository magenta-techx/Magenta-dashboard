import { Types } from "./type";
export const INITIAL_STATE = {
  stat: '',
};
const reducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
